{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome against localhost",
            "url": "http://127.0.0.1:8887/",
            //"url": "http://127.0.0.1:8887/freetest/index.html",
            "webRoot": "${workspaceFolder}", 
            "skipFiles": [
                "${workspaceFolder}/libjs/*.js"
                ]
        }
    ]

    // "webRoot" est utile pour le débugage, il n'est pas en doublon avec "url"

}